fn main() {
    println!("Hello, world!"); // ! is needed to call a macro, not a function
                               // What is macros will be discussed later
                               // without the ! it would be a function call
}

// there is a tool 'rustfmt' that formats all file automaticly
// 'rustc' to compile - fair enough
